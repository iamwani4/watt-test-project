enum Gender {
    MALE
    FEMALE
    OTHER
}

model Application {
    id        Int      @id @default(autoincrement())
    name      String
    email     String   @unique
    gender    Gender?
    age       Int
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}
